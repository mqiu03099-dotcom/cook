<template>
  <div
    class="gap-6 grid grid-cols-[repeat(auto-fill,minmax(300px,1fr))] md:grid-cols-[repeat(auto-fill,minmax(280px,1fr))] lg:grid-cols-[repeat(auto-fill,minmax(380px,1fr))]"
  >
    <slot></slot>
  </div>
</template>

<script lang="ts" setup></script>

<style scoped></style>
